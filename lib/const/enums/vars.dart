enum NewsType {
  topTrending,
  allNews,
}

enum SortByEnums {
  relevancy, //  articles more relevant to the user
  popularity, //  articles from popular sources ans publishers come first
  publishedAt, //  articles published more recently , newest articles come first
}
